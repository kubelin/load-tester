# 부하테스트 결과서 작성 가이드

"서버가 견고하다"는 주장을 **숫자와 증적**으로 증명하는 문서를 만드는 방법.
결과서의 논리 구조는 항상 같다: **측정이 유효했고(캘리브레이션) → 목표 조건에서 기준을 충족했으며(시나리오별 지표) → 증거가 있다(증적).**

---

## 1. 핵심 지표 정의 — 결과서에 쓰는 숫자들

| 지표 | 정의 | 왜 보나 | 어디서 뽑나 |
|---|---|---|---|
| **TPS (정상상태)** | 램프업 제외 구간의 초당 처리 건수 | 처리량 요구 충족 여부 | `summarize-jtl.sh` / HTML 리포트 |
| **에러율** | 실패 응답 비율 (HTTP 에러 + 커넥션 실패 + assertion 실패) | 견고함의 1차 지표 | 〃 |
| **응답시간 p95 / p99** | 95%·99% 유저가 경험한 응답시간 상한 | 평균은 속는다 — 꼬리가 진짜 품질 | 〃 |
| **응답시간 최대 / 추이** | 스파이크 유무, 시간에 따른 상승 여부 | 순간 끊김·누적 열화 검출 | HTML 리포트 그래프 |
| **서버 CPU / 메모리** | 부하 중 서버 자원 사용률 | 여유율 = 앞으로의 성장 여력 | 서버에서 top/sar |
| **procUs (서버 내부 처리시간)** | 요청 수신→응답 직전 (μs) | 지연을 서버/네트워크로 분해 | 서버 access 로그 |

**평균만 쓰지 말 것.** "평균 5ms"여도 p99가 2초면 100명 중 1명은 2초를 기다린 것 — 결과서의 응답시간은 반드시 avg + p95 + p99 + max 세트로 쓴다.

---

## 2. 시나리오별 합격 기준 (결과서의 판정 근거)

프로젝트 SLA가 있으면 그 값을 쓰고, 없으면 아래 기본값으로 시작한다.

| 시나리오 | 실행 | 합격 기준 (예시) |
|---|---|---|
| **A. 캘리브레이션** | `run-calibration.sh <IP> <PORT> <목표TPS>` | 발생 능력 ≥ 목표×1.3, 에러 0% → **이후 측정의 유효성 확보** |
| **B. 목표 TPS 유지** | `run-target-tps.sh 10000 <IP> <PORT> 300` | TPS 목표±2%, 에러율 ≤ 0.1%, p99 ≤ SLA(예 100ms), 5분 이상 유지, 서버 CPU ≤ 70% |
| **C. 최대 TPS (용량)** | `run-max-tps.sh <IP> <PORT>` | 최대치 ≥ 목표×1.5 (여유율 증명), 한계 도달 시 **에러가 아닌 지연으로 완만히 저하**(graceful) |
| **D. vUser 시나리오** | `run-vusers.sh 5000 <IP> <PORT> 1000 300` | 목표 동시 유저에서 에러 0%, p99 안정, TPS ≈ 예상치(공식 ①) |
| **E. 지속(soak)** | `run-vusers.sh <N> <IP> <PORT> 1000 3600` | 1시간 동안 지표 무열화: TPS·p99 추이 평탄, 서버 메모리 증가 없음(누수 검증) |

B가 "요구사항 충족", C가 "여유율", E가 "오래 버팀" — **견고함의 증명은 B+C+E 세 개가 한 세트**다. D는 요구가 동시 유저 수로 주어졌을 때 추가.

---

## 3. 부하 조합 설계 — 같은 TPS라도 유저 수가 다르면 다른 시험이다

테스트를 계획하는 사람이 제일 먼저 정할 것은 "몇 TPS"가 아니라 **"어떤 조합으로 그 TPS를 만들 것인가"**다.
부하에는 두 축이 있다:

```
처리량 축 (TPS)      : 서버가 초당 얼마나 빨리 쳐내는가
동시성 축 (vUser)    : 서버가 동시에 몇 개의 세션/커넥션을 유지하는가
연결 고리 = think time (공식 ①: TPS = vUser ÷ (RT + think))
```

### 같은 10,000 TPS를 만드는 세 가지 조합 — 각각 다른 것을 시험한다

| 조합 | 커넥션 | 시험하는 것 | 주 부담 | **주시할 지표** |
|---|---|---|---|---|
| **500유저 × think 0** (고회전) | 500 | 순수 처리 속도 | CPU, 처리 지연, GC | **p99·max 응답시간, 서버 CPU** |
| **5,000유저 × think 0.45s** (중간) | 5,000 | 처리 + 동시 세션 | 워커 점유, keep-alive 정책 | p99·CPU(고회전 지표 유지) + **커넥션 에러 추가 감시** |
| **20,000유저 × think 1.95s** (저회전) | 20,000 | 동시 세션 유지 | FD, 커넥션당 메모리, idle 관리 | **서버 메모리·FD 사용량, 끊김 에러** |

커넥션 에러란: `Connection refused`(연결 수립 거절 — accept 큐·최대 커넥션 한도)와
`Connection reset`(맺어진 연결을 서버가 끊음 — 유휴 정리 정책·워커 고갈). 커넥션이
수천 개로 늘어나는 순간부터 서버의 "커넥션 수용·유지 정책"이 시험대에 오르므로 추가된다.

같은 TPS인데 결함이 드러나는 지점이 다르다. 실제 사례: 워커 200개 Java 서버는
①번 시험은 통과하지만(동시 처리 수십 건이면 충분) ③번에서 유휴 커넥션 정책 때문에
대량 실패했다 — **TPS 지표만 보는 테스트는 이런 결함을 놓친다.**

### 계획 절차 — 요구사항 유형별

```
요구가 "N TPS"로만 옴
  → 실제 유저의 think time을 추정해 vUser를 역산 (공식 ①)
  → 그 조합(현실형) + 500유저급 고회전(처리량형) 두 가지를 돌린다

요구가 "동시 유저 N명"으로만 옴
  → think time 추정으로 예상 TPS 계산 → D 시나리오가 주 시험
  → 서버 쪽 지표(메모리·FD·커넥션)를 반드시 수집

요구가 "유저 N명 + M TPS" 둘 다 옴
  → vUser = N 고정, think = N/M − RT 로 맞춤 (조합이 하나로 결정됨)
```

### 결과서에 쓸 때의 규칙

- 모든 시나리오에 **조합 3요소(vUser / think / 달성 TPS)를 반드시 병기**한다.
  "10,000 TPS 합격"만 쓰면 어떤 시험이었는지 알 수 없다.
- 처리량형 결과에는 응답시간·CPU를, 동시성형 결과에는 커넥션·메모리 지표를
  중심으로 기술한다 (§2 기준표의 항목을 조합 유형에 맞게 가감).
- **완전한 증명 = 고회전 1개 + 저회전 1개** — 같은 TPS를 두 조합으로 통과해야
  "처리량도 되고 동시성도 된다"가 성립한다.

---

## 4. 지표 수집 방법 (테스트 실행 시 함께 할 일)

### 4-1. 클라이언트 지표 — 자동 (jtl)

테스트가 끝나면 램프업을 제외하고 요약:

```bash
./scripts/summarize-jtl.sh results/result_target10000_<시각>.jtl 30   # 앞 30초 제외
```

출력되는 TPS/에러율/avg/p50~p99/max를 결과서 표에 그대로 옮긴다.
그래프 증적은 `REPORT=1`로 실행했거나 `jmeter -g <jtl> -o report/`로 생성한 HTML 리포트에서.

### 4-2. 서버 지표 — 테스트 시작 전에 수집을 걸어둔다

서버 VM에서 (테스트 시작 직전):

```bash
# CPU/메모리 5초 간격 기록 (테스트 끝나면 Ctrl-C)
top -b -d 5 | grep -E "^%Cpu|KiB Mem|MiB Mem" > server_top_$(date +%m%d%H%M).log &

# 또는 sar가 있으면 (sysstat 패키지)
sar -u -r 5 > server_sar_$(date +%m%d%H%M).log &
```

결과서에는 **정상상태 구간의 CPU 평균·피크**와 **메모리 추이(soak에서 증가 여부)**를 기록.
VM이면 `%st`(steal)도 함께 — 값이 크면 "서버 한계"가 아니라 "이웃 VM 간섭"이다.

### 4-3. 서버 내부 처리시간 — access 로그에서 procUs 분포

서버를 `-logdir`로 띄웠다면:

```bash
# procUs 평균/최대 (μs)
awk '{for(i=1;i<=NF;i++) if($i~/^proc_us=/){sub("proc_us=","",$i); s+=$i; n++; if($i>mx)mx=$i}}
     END{printf "procUs avg=%.0f max=%d (n=%d)\n", s/n, mx, n}' logs/access_*.log
```

**지연 분해 공식**: JMeter가 본 응답시간 − procUs ≈ 네트워크 + 대기열.
p99가 높은데 procUs가 낮으면 서버 로직이 아니라 커넥션/네트워크 쪽 문제라는 증거가 된다.

---

## 5. 결과서 양식 (복사해서 사용)

```markdown
# ○○ 서버 부하테스트 결과서

## 1. 개요
| 항목 | 내용 |
|---|---|
| 목적 | ○○ 서버의 1만 TPS 처리 능력 및 안정성 검증 |
| 일시 / 수행자 | 2026-09-○○ / ○○○ |
| 대상 서버 | RHEL ○.○, ○코어/○○GB, <IP>, 앱 버전 ○○ |
| 부하 발생기 | RHEL ○.○, ○코어/○○GB, JMeter 5.6.3, JDK 17 |
| 네트워크 | 10G NIC, 동일 세그먼트 |
| 판정 기준 | 에러율 ≤ 0.1%, p99 ≤ ○○ms, 목표 TPS ±2% (§근거: SLA 문서/기본값) |

## 2. 측정 유효성 (캘리브레이션)
| 항목 | 결과 | 기준 | 판정 |
|---|---|---|---|
| 발생기 최대 능력 | ○○,○○○/s | ≥ 목표×1.3 (13,000) | 합격 |
→ 이하 모든 측정에서 발생기는 병목이 아님.

## 3. 시나리오별 결과
### 3-1. 목표 TPS 유지 (1만 TPS × 5분)
| 지표 | 결과 | 기준 | 판정 |
|---|---|---|---|
| TPS (정상상태) | ○○○○/s | 10,000 ±2% | |
| 에러율 | ○.○○% (○건/총 ○○건) | ≤ 0.1% | |
| 응답 avg / p95 / p99 / max | ○/○/○/○ ms | p99 ≤ ○○ms | |
| 서버 CPU 평균 / 피크 | ○○% / ○○% | ≤ 70% | |
| 서버 procUs 평균 | ○○μs | (참고) | |

### 3-2. 최대 TPS (용량)  … 단계표: 스레드→TPS, 정체 지점, 한계 시 거동
### 3-3. vUser ○,○○○명   … 동일 표 형식
### 3-4. 지속 1시간(soak) … TPS/p99/메모리 추이 그래프 + "열화 없음" 판정

## 4. 종합 판정
목표 1만 TPS를 에러율 ○%, p99 ○ms로 5분간 유지했고, 최대 용량은 ○만 TPS로
목표 대비 ○○% 여유를 확인함. 1시간 지속 시 지표 열화 없음. → **합격**

## 5. 증적 목록
- HTML 리포트: report_target10000/ (TPS·응답시간 그래프)
- 원시 데이터: result_*.jtl (요청 단위 전수 기록)
- 서버 자원: server_top_*.log / 서버 처리시간: logs/access_*.log
- 실행 환경: check-kernel-loadgen.sh 출력 (커널 설정 증빙)
```

---

## 6. 작성 순서 요약

```
① 테스트 전: 서버에서 top/sar 수집 시작 + 서버를 -logdir로 기동
② 실행: 시나리오 A → B → C → D → E 순서로 (각각 REPORT=1)
③ 수치화: summarize-jtl.sh <jtl> <램프업초> → 표에 기입
④ 서버 지표: top 로그에서 CPU 평균/피크, access 로그에서 procUs
⑤ 판정: 기준표와 대조해 시나리오별 합격/불합격 기입
⑥ 증적 첨부: HTML 리포트의 "Transactions Per Second"와
   "Response Times Over Time" 그래프 캡처 2장이 최소 세트
```

**그래프 읽는 법 (증적 캡처 포인트)**: B 시나리오라면 TPS 그래프는 목표값에서
평평해야 하고, 응답시간 그래프도 평평해야 한다. 응답시간이 우상향하면 어딘가
누적되고 있다는 뜻(커넥션·큐·메모리)이고, 톱니형 스파이크는 GC나 주기성 간섭 의심 —
이런 패턴이 보이면 "합격이지만 관찰 필요"로 기록한다.
