# 부하테스트 계산 공식 치트시트

설계는 공식으로, 확정은 실측(캘리브레이션)으로. 이 순서가 전부다.

## ① 기본 관계식 (Little의 법칙) — 제일 많이 씀

```
TPS = 동시 유저 수 ÷ 1인당 소요시간(초)
    = vUser ÷ (응답시간 + think time)
```

세 변수 중 둘을 알면 나머지가 나온다:

| 구하려는 것 | 공식 | 예시 |
|---|---|---|
| TPS | vUser ÷ (RT + think) | 5,000명 ÷ (0.05 + 1)초 ≈ **4,760 TPS** |
| 필요 vUser | TPS × (RT + think) | 1만 TPS × 0.05초 = **500명** |
| 허용 응답시간 | vUser ÷ TPS − think | 500명 ÷ 1만 TPS = **50ms** (think 0일 때) |

## ② 스레드 산정 (JMeter 설정용)

```
필요 스레드 = 목표 TPS × 응답시간(초) × 2      ← ×2는 타이머 지터 여유분
```

예: 응답 100ms API에 1만 TPS → 10,000 × 0.1 × 2 = **2,000 스레드**

목표 미달 + 에러 0%면 스레드 부족 신호(전부 응답 대기 중) → 이 공식으로 재계산.

## ③ TPS ↔ RPS 변환

```
RPS = TPS × 트랜잭션당 요청 수
```

예: "결제 500 TPS" 요구, 결제 1건 = API 4회 → 발생기는 **2,000 RPS** 필요.
단일 API 테스트(1건 = 1요청)면 TPS = RPS.

## ④ 대역폭 체크 (불가능 조합 필터)

```
필요 대역폭(Gbps) ≈ 트랜잭션 크기(KB) × TPS(만 단위) × 0.08
```

예: 10KB × 2만 TPS → 10 × 2 × 0.08 = **1.6 Gbps** (10G NIC의 16%, OK)
상세 유도와 조견표는 TUNING.md 5장.

## ⑤ 신규 커넥션 상한 (Keep-Alive 안 쓸 때만)

```
초당 최대 신규 연결 = 임시 포트 수 ÷ TIME_WAIT(초)
```

예: 포트 28,000개 ÷ 30초 ≈ **933 conn/s**
이걸 넘는 시나리오 → keep-alive 켜거나 포트 범위/TIME_WAIT 튜닝 (TUNING.md 1장).

## ⑥ 하루 트래픽 → 목표 TPS 역산 (요구사항 도출용)

```
평균 TPS = 일 건수 ÷ 86,400
피크 TPS = 평균 × 피크 배수 (통상 3~10, 이벤트성 트래픽은 그 이상)
```

예: 일 1억 건 → 평균 1,157 TPS → 피크 배수 5 → **목표 ~6,000 TPS**

## ⑦ 동시 커넥션 수 (FD 한도 산정용)

```
동시 커넥션 ≈ TPS × 응답시간(초)          (keep-alive + 페이싱 모델)
동시 커넥션 = vUser 수                      (vUser마다 커넥션 유지 모델)
```

예: 2만 TPS × 0.1초 = 2,000 커넥션 → `ulimit -n` 65,536이면 여유.

## ⑧ vUser ↔ 실 동접자 환산 (테스트 결과 → 운영 용량 해석)

**JMeter vUser 수 ≠ 실 동접자 수.** vUser는 쉬지 않고 요청하지만, 실유저는 대부분의
시간을 화면 보고 입력하는 데 쓴다. 그래서 vUser 1명은 실유저 여러 명을 대표한다.

```
실 동접 추정 = 감당 TPS ÷ 실유저 1명의 초당 요청수
            = 감당 TPS ÷ (1 ÷ 실유저 요청간격초)
```

**TPS가 진짜 용량 지표**다. vUser 수가 아니라 "에러 없이 처리한 TPS"를 기준으로 환산한다.

예: 테스트가 2만 TPS를 무결점 처리했을 때, 실유저 행동 패턴별 커버 가능 동접:

| 실유저 요청 간격 | 유저당 초당 요청 | 커버 동접 (2만 TPS 기준) |
|---|---|---|
| 1초 (봇 수준, 워스트) | 1.0 | 2만 명 |
| 5초 (활발한 사용) | 0.2 | 10만 명 |
| 30초 (일반 브라우징) | 0.033 | 60만 명 |
| 3분 (가끔 클릭) | 0.0055 | 360만 명 |

주의:
- 결과서에는 "vUser 2만 = 동접 2만"이라 쓰지 말 것. **"피크 X TPS 처리 = 동접 Y명
  커버 (유저당 Z req/s 가정)"** 형태로, 가정을 명시해 쓴다.
- 실유저 요청간격은 운영 로그/APM에서 뽑는다 (세션당 요청수 ÷ 세션 길이).
- **게이트웨이·커넥션 유지형** 대상은 예외: 병목이 TPS가 아니라 동시 유지 커넥션이면
  "동접 = 동시 커넥션"에 가까워, vUser 수가 실동접에 직접 대응한다 (⑦ 참고).

---

## 이 레포의 jmx와 공식 매핑

| 플랜 | 부하 모델 | think time | 쓰는 공식 |
|---|---|---|---|
| `target-tps.jmx` | 고정 TPS (타이머 페이싱) | 없음 (타이머가 대체) | ①② — 스레드 산정 |
| `echo-load.jmx` | 폐쇄 루프 (vUser 시뮬레이션) | `-Jthinkms`로 조절 (0=무휴식) | ① — vUser↔TPS 환산 |
| `calibration.jmx` | 폐쇄 루프 (발생기 검증) | `-Jthinkms`로 조절 | ①⑤ |
